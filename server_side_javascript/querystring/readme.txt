http://a.com/topic?id=1 => 이것을 URL이라고 부른다. 여기서 id=1인 부분이 query string부분이다.
http(protocol에 해당) a.com(domain에 해당) topic(path에 해당)

query string은 하나의 path로 id의 값이 무엇이냐에 따라 다양한 화면을 출력해 낼 수 있는 장점을 가지고 있다.
